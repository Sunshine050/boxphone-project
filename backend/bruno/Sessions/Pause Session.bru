meta {
  name: Pause Session
  type: http
  seq: 7
}

post {
  url: {{base_url}}/sessions/{{session_id}}/pause
  body: json
  auth: bearer
}

auth:bearer {
  {{admin_token}}
}

body:json {
  {
    "reason": "Device disconnected - testing"
  }
}

