meta {
  name: Connect User to Device
  type: http
  seq: 6
}

post {
  url: {{base_url}}/users/{{user_id}}/connect-device
  body: json
  auth: bearer
}

auth:bearer {
  {{admin_token}}
}

body:json {
  {
    "device_id": "{{device_id}}"
  }
}

